package leetcode

class Solution877 {
    fun stoneGame(piles: IntArray): Boolean {
        // There is always even piles
        // So Alex can calculate sum of all odd-indexed and even-indexes
        // If odd is larger take the first pile, and continue taking odd indexes
        // If even is larger take the last pile, and continue taking even indexes
        return true
    }
}